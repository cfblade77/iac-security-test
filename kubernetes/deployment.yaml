# VULNERABLE - runs as root, privileged, no resource limits
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-service-app
  template:
    metadata:
      labels:
        app: api-service-app
    spec:
      containers:
      - name: api-container
        image: nginx:latest
        securityContext:
          privileged: true
          runAsUser: 0
        env:
        - name: AWS_SECRET_KEY
          value: "xYz987UVWtSRQpOnMLkjIHGFEDCBA/aBcDeFgHiJ"
        - name: DB_PASSWORD
          value: "T3stP@ssw0rd!442"